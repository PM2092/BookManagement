Requirements for running program :

Need to have MySQL running in background
Change line #16 in "BookDB.java" with appropriate database name username and password.
Create Table with name used is BookDB.

Table should have couple of records to display record when you run program for first time.
